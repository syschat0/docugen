# DocuGen 개선 로드맵

다양한 유형의 글 지원을 목표로 한 우선순위별 진행목록.
(2026-07-08 분석 기준. M1은 범위 확정, M2 이후는 착수 시 세부 설계.)

---

## M1. 참고문헌 표기법 2종 지원 — 확정, 즉시 착수 가능

소스가 사실상 100% URL 형식이므로 메타데이터 추출 없이 렌더링 분기만으로 구현.
다른 마일스톤에 의존하지 않는 독립 작업.

- [x] 1-1. `citation_style` 프로젝트 설정 추가 (`numeric` | `author_date`, 기본 `numeric`)
      — `app/schemas/projects.py`, `app/db/repositories.py`(get/set_project_settings), `app/api/settings.py`
- [x] 1-2. 번호식 렌더러 정비: 현행 `[[n]](url)` + 참고문헌 목록에 사이트명·접속일 추가
      — `app/services/citations.py` (접속일 = research 아티팩트 `updated_at`)
- [x] 1-3. 저자-연도식 렌더러: 본문 `(사이트명, n.d.)` 링크 + 가나다/알파벳순 목록
      — 동일 도메인 다건 시 `n.d.-a`, `n.d.-b` 접미사 구분, `www.` 제거, `file://` 참조는 `[사용자 제공 자료]`로 표기
- [x] 1-4. 이음새 가드 확장: `global_citation_numbers()` 정규식이 저자-연도 링크 패턴도 인식
      — `app/services/citations.py:53`, `smooth_chapter_seams` 경로
- [x] 1-5. `final_merge`에 스타일 주입 + 스타일 변경 시 캐시 유지한 채 merge만 재실행
      — `app/db/repositories.py` (`_local_merge`, draft 재생성 조건)
- [x] 1-6. 설정 UI에 표기법 토글 추가
      — `app/static/index.html`, `app/static/app.js` (기존 프로젝트 설정 패널)
- [x] 1-7. 테스트: 두 스타일 렌더링, n.d. 접미사 규칙, file 참조, 이음새 가드
      — `tests/test_citations.py` 확장
- [x] 1-8. (선택 후속) 메타태그·JSON-LD 파싱으로 저자/게시일 있는 소스 승격
      — `(사이트명, n.d.)` → `(저자, 연도)`. `app/services/page_meta.py` 신설,
      browser/HTTP/챕터 리서치 fetch 경로 연결 (사용자 URL 참조는 DB 스키마
      변경이 필요해 제외 — 도메인 폴백 유지)

## M2. 문서 유형(장르) 기반 마련 — 유형 다양화의 토대

- [x] 2-1. `document_type` 필드 추가 (프로젝트 생성/수정 스키마·DB·API)
      — `session.py` 컬럼 마이그레이션 헬퍼, `GET /doc-types` 엔드포인트
- [x] 2-2. 유형 프로파일 레지스트리: `app/services/doc_types.py`
      (1차 유형 6종: 보고서(현행) / 소논문(학교 과제) / 블로그·칼럼 / 에세이
      / 기술 문서 / 발표 대본 — 리서치 기본값, 인용 on/off, 제목 번호,
      기본 분량, 문체, 단계별 프롬프트 가이드 선언)
- [x] 2-3. 인테이크에서 유형 자동 분류(best-effort) + UI에서 확인·변경
      — 유형 미지정 시 실행 시작에 분류 후 저장, 변경 시 파이프라인 무효화
- [x] 2-4. 프로젝트 생성 UI 유형 선택(자동 감지 포함) + 생성 조건 패널에서 변경
      — M2에서 함께 배선된 것: brief/outline/섹션 프롬프트 유형 가이드 주입,
      인용 없는 유형(에세이·발표 대본)의 마커 제거·Sources 생략,
      제목 번호 유형별 적용, 에세이 리서치 기본 off

## M3. 파이프라인 스테이지 게이팅 — M2 의존

- [x] 3-0. 스테이지 순서·레이블·무효화·요약 초기화 규칙을
      `PipelineStageSpec` 레지스트리로 통합. `feedback_revision` 강제 실행도
      해당 단계 이후 실행 로그를 정확히 초기화
- [x] 3-1. `run_document_generation`(~1,200줄 단일 함수)을 스테이지 레지스트리 구조로 분해
      - [x] 3-1a. `final_merge`의 인용 조립·이음새·품질 요약을
        부작용 없는 `_build_final_draft_content`로 추출
      - [x] 3-1b. 대기 질문 처리와 문서 유형·프로파일·사용자 결정 준비 로직 추출
      - [x] 3-1c. 인테이크 질문 계획·대기·실패 처리 블록을 `_run_intake_stage`로 추출
      - [x] 3-1d. 스타일 샘플 재사용·생성·실패 허용 로직을 `_run_style_card_stage`로 추출
      - [x] 3-1e. 검색·출처 요약·참조 병합을 `_run_research_stage`와 `ResearchStageResult`로 추출
      - [x] 3-1f. brief 생성·재사용·문체 적용·길이 예산 계산을 `_run_brief_stage`로 추출
      - [x] 3-1g. outline 생성·검토·수정 적용·승인 대기를 `_run_outline_stage`로 추출
      - [x] 3-1h. section plan 생성·검토·재확장·길이 재배분을 `_run_section_plan_stage`로 추출
      - [x] 3-1i. 장별 출처 생성·재사용을 `_run_chapter_research_stage`로 추출
      - [x] 3-1j. 최신 섹션 초안·요약 재사용 경로를 `_reuse_section_writing_stage`로 추출
      - [x] 3-1k. 단일 섹션 작성·요약·근거 검증/복구를 `_write_planned_section`으로 추출
      - [x] 3-1l. 초안 이후 섹션 피드백 적용을 `_run_feedback_revision_stage`로 추출
      - [x] 3-1m. 연속성·유형별 루브릭 검토를 `_run_document_review_stage`로 추출
      - [x] 3-1n. 검토 기반 타깃 수정·문장 품질 제한 복구를 `_run_targeted_revision_stage`로 추출
      - [x] 3-1o. 신규 섹션 순회·저장·장 요약·출처 보강을 `_run_new_section_writing_stage`로 추출
- [x] 3-2. 유형 프로파일에 따라 리서치·인용 스테이지 조건부 실행
      — `research_default=false` 유형은 웹·장별 리서치를 명시적으로 건너뛰되
      사용자 첨부 자료는 유지. 전역 검색 차단은 프로젝트 재정의보다 우선.
      `citations_enabled=false` 유형은 인용·출처 렌더링을 생략하고 버전 조건에도 표시.

## M4. 구조·분량 규칙 유형별화 — M2 의존

- [x] 4-1. 제목 번호 매김을 유형별 옵션으로 — M2에서 `numbered_headings`로 반영.
      하위 제목 금지 규칙은 계층 아웃라인 트리가 구조 분할을 담당하므로
      전 유형 공통 유지로 판단 (유형별 옵션 불필요)
- [x] 4-2. 문서 전체 목표 분량 → 섹션별 배분
      — 우선순위: 설정 패널 `target_length` > brief가 요청문에서 추출한
      `target_length_chars` > 없음. 배분은 실행 시 비례 재계산(저장 안 함,
      섹션당 150~3,000자 클램프), outline/챕터 확장 프롬프트에도 예산 전달.
      normalize의 500자 하드코딩이 프로파일 기본 분량을 가리던 문제 수정

## M5. 문체·인테이크 심화 — M2 의존

- [x] 5-1. 유형별 인테이크 질문 은행을 프로파일의 `intake_priorities`로 선언
      — 유형별 핵심 누락 정보 4개를 우선순위대로 주입하되, 요청·기존 답변에서
      추론 가능한 항목은 건너뛰고 체크리스트 전체를 기계적으로 묻지 않도록 제한
- [x] 5-2. 스타일 카드: 어체·어조·시점·시제·문장 리듬·어휘 + 퓨샷 예문을
      `style_card` 아티팩트로 — 섹션 작성 프롬프트에 주입, 어체(register)는
      brief.style을 오버라이드해 수정·이음새 프롬프트에도 전파
- [x] 5-3. 샘플 글 업로드 → 스타일 카드 추출 — references를 `kind="style"`로
      확장(연구 소스 풀에서 제외, 인용 안 됨), 파이프라인에 `style_card`
      스테이지 추가(샘플 없으면 skip, 실패해도 카드 없이 계속)

## M6. 품질·연속성 고도화 — M2 의존

- [x] 6-1. 장르 인지 메모리: 프로파일별 `memory_schema`를 섹션 핸드오프와
      장 요약에 적용. 서사형은 전개·인물/이미지·시점/분위기, 논증형은
      논지 상태·근거 체인·반론, 절차형은 완료 단계·시스템 상태·실패 분기를
      짧게 유지하며 다른 장르의 메모리 필드는 전달하지 않음
- [x] 6-2. 루브릭 평가 스테이지 — 유형별 4기준(프로파일 `rubric` 선언)을
      챕터 단위로 1~5점 채점하는 `rubric_review` 스테이지 추가.
      연속성 검토 결과와 병합(타깃 합산·중복 제거·최대 5개 캡)해
      `targeted_revision` 한 번으로 수정. 전 과정 best-effort로
      루브릭 실패가 실행을 막지 않음
- [x] 6-3. 스테이지×유형별 생성 파라미터
      — 프로파일 `generation_params`에 intake/brief/outline/section plan/작성/
      요약/검토/수정별 temperature·max_tokens 선언. 기술 문서는 보수적으로,
      에세이·블로그·발표는 작성 다양성을 높이고 검토·요약은 전 유형 낮게 유지.
      컨텍스트 전달로 JSON 복구 호출도 동일 설정을 상속하고 호출 후 자동 초기화
- [x] 6-4. 결정적 근거 품질 계층
      — 출처 신뢰도 등급, 고위험 주제 권위 출처 보강 검색, 관련 원문 passage,
      evidence ledger 검증과 1회 보정, 인용·출처·검토 결과를 통합한 품질 요약
- [x] 6-5. 문장·구조 품질 게이트와 제한적 보정
      — 반복·상반 진술·무근거 단정, 유형별 긴 문장·문단·목록 편중,
      제목 구조와 보고서·소논문 도입/결론 검사. 최대 3개 섹션 자동 보정은
      이슈 감소·제목·인용·근거 검증을 모두 통과할 때만 채택
- [x] 6-6. 품질 검토 UX
      — 이슈 위치 이동·강조, 초안 버전별 확인/예외 결정과 사유 기록,
      활성 경고·프로젝트 상태 재계산, 이슈를 섹션 피드백으로 자동 입력,
      저장 후 `feedback_revision`부터 해당 섹션 개선 실행

## M7. 품질 벤치마크 — 다음 우선순위

- [x] 7-1. 6개 문서 유형별 대표 요청·기대 특성 fixture 구축
- [x] 7-2. 결정적 품질 지표를 동일 포맷으로 집계하는 오프라인 실행기
- [x] 7-3. 기준 초안과 후보 초안의 지표 차이·회귀 판정 리포트
- [x] 7-4. 실제 SLM 생성 전후 비교와 사람 평가 기록 포맷
      — 프로젝트 섹션 초안을 케이스별 JSON으로 내보내고 before/after 결정 지표를
      동일 포맷으로 비교. A/B 순서를 케이스별로 블라인드한 평가표와 별도 키 생성,
      과업 충족·구조·연속성·장르 적합·가독성·근거성 6개 1~5점 및 선호·사유 기록.
      완료 평가표를 다시 입력하면 실제 버전별 평균과 선호도 집계

---

### 의존 관계 요약

```
M1 (독립) ──────────────────┐
M2 ─→ M3 ─→ M4              ├─→ 완성: 다양한 유형 + 표기법 지원
      └──→ M5, M6 ──────────┘
```
